var express = require('express');
var router = express.Router();



/* GET users listing. */
router.get('/', (req, res, ) => {
    res.send('respond with a resource');
  });

//CREATION, SUPPRESSION, MISE A JOUR (recette, note, autre?)

  module.exports = router;